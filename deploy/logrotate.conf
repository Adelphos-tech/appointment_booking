/var/log/slotcare/*.log {
    daily
    rotate 14
    compress
    delaycompress
    missingok
    notifempty
    create 0644 root root
    sharedscripts
    postrotate
        if command -v pm2 &> /dev/null; then
            pm2 flush slotcare 2>/dev/null || true
        fi
    endscript
}
